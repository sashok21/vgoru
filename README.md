# 🏔️ Гірські маршрути України

Веб-платформа для любителів гірського туризму та походів. Тут ви можете знайти детальні описи маршрутів, залишати відгуки, ділитися своїм досвідом та відслідковувати пройдені вершини.

## 📋 Про проект

Сайт створений для того, щоб зробити гірський туризм в Україні доступнішим та цікавішим. Ми збираємо інформацію про найкращі маршрути Українських Карпат, а користувачі діляться своїми враженнями та порадами.

### Основні можливості:
- 📍 Каталог гірських маршрутів з детальною інформацією
- ⭐ Система оцінювання та відгуків від туристів
- 👤 Персональні профілі користувачів
- ❤️ Додавання маршрутів в улюблені
- ✅ Відстеження пройдених маршрутів
- 🗺️ Інтерактивні карти маршрутів
- 🔍 Фільтрація маршрутів за складністю, висотою та регіоном

---

## 🚀 Як запустити проект

### Вимоги перед стартом:
- Python 3.8 або вище
- pip (менеджер пакетів Python)
- Git (опціонально)

### Крок 1: Клонування або завантаження проекту

Якщо у вас є Git, клонуйте репозиторій:
```bash
git clone <URL-вашого-репозиторію>
cd vgoru
```

Або просто завантажте файли проекту та розпакуйте їх.

### Крок 2: Створення віртуального оточення

Це важливо! Віртуальне оточення ізолює залежності проекту від вашої системи.

На **Windows**:
```bash
python -m venv venv
venv\Scripts\activate
```

На **macOS/Linux**:
```bash
python3 -m venv venv
source venv/bin/activate
```

Якщо успішно, у вашому терміналі має з'явитися `(venv)` на початку рядка.

### Крок 3: Установка залежностей

Перейдіть в папку проекту (якщо ви там уже не знаходитесь) та встановіть потрібні пакети:

```bash
pip install django pillow
```

Розшифровка:
- `django` — фреймворк для розробки цього сайту
- `pillow` — бібліотека для роботи з зображеннями

### Крок 4: Применяння міграцій бази даних

Цей крок створить потрібні таблиці в базі даних:

```bash
python manage.py migrate
```

### Крок 5: Створення адміністратора (суперкористувача)

Ви можете управляти сайтом через панель адміністратора Django. Для цього потрібно створити акаунт:

```bash
python manage.py createsuperuser
```

Вам запропонують ввести:
- **Username** (ім'я користувача) — наприклад: `admin`
- **Email** — ваша електронна адреса
- **Password** (пароль) — придумайте надійний пароль
- **Password (again)** — повторіть пароль

### Крок 6: Запуск сервера

Тепер ви можете запустити розробницький сервер Django:

```bash
python manage.py runserver
```

Ви повинні побачити щось подібне:
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CONTROL-C.
```

### Крок 7: Відкриття сайту

Відкрийте браузер і перейдіть за адресою:
- **Головна сторінка**: http://127.0.0.1:8000/
- **Панель адміністратора**: http://127.0.0.1:8000/admin/

Для входу в панель адміністратора використовуйте дані, які ви вводили при створенні суперкористувача на кроці 5.

---

## 📦 Структура проекту

```
vgoru/                          # Головна папка проекту
├── mountains_roads/            # Основна папка додатку
│   ├── migrations/            # Міграції бази даних
│   ├── static/                # Статичні файли (CSS, JS)
│   │   └── mountains_roads/
│   │       ├── css/           # Стилі сайту
│   │       └── js/            # JavaScript скрипти
│   ├── templates/             # HTML шаблони
│   │   └── mountains_roads/   # Шаблони додатку
│   ├── models.py              # Моделі бази даних
│   ├── views.py               # Логіка сторінок
│   ├── forms.py               # Форми для користувачів
│   └── urls.py                # Маршрути сайту
├── vgoru/                      # Параметри проекту
│   ├── settings.py            # Основні налаштування
│   └── urls.py                # Основні маршрути
├── manage.py                  # Команди управління Django
├── db.sqlite3                 # База даних (створюється після migrate)
└── README.md                  # Цей файл
```

---

## 🎨 Функціонал сайту

### Для не авторизованих користувачів:
- Перегляд каталогу маршрутів
- Фільтрація маршрутів за різними параметрами
- Перегляд відгуків інших користувачів
- Перегляд профілів користувачів
- Реєстрація та вхід на сайт

### Для авторизованих користувачів:
- Все з вищезазначеного, плюс:
- Залишання відгуків до маршрутів
- Редагування та видалення своїх відгуків
- Додавання маршрутів в улюблені
- Позначення маршрутів як пройдених
- Редагування профілю з аватаром та описом

### Для адміністраторів:
- Додавання нових маршрутів
- Редагування інформації про маршрути
- Видалення маршрутів та відгуків
- Управління користувачами
- Переглядання статистики

---

## 🔧 Корисні команди Django

Якщо вам потрібно виконати якіс додаткові операції:

```bash
# Запуск сервера на іншому порту (якщо 8000 зайнято)
python manage.py runserver 8080

# Створення нової міграції (якщо ви змінили моделі)
python manage.py makemigrations

# Застосування міграцій
python manage.py migrate

# Вхід в інтерактивну оболонку Python з доступом до моделей
python manage.py shell

# Видалення всіх даних з бази (обережно!)
python manage.py flush

# Запуск тестів
python manage.py test
```

---

## 🐛 Розв'язування проблем

### Помилка: "No module named 'django'"
**Рішення**: Ви забули встановити залежності або не активували віртуальне оточення.
```bash
pip install django pillow
```

### Помилка: "port 8000 is already in use"
**Рішення**: Чи запустіть сервер на іншому порту, або закрийте програму, що використовує цей порт:
```bash
python manage.py runserver 8080
```

### База даних не створена
**Рішення**: Запустіть міграції:
```bash
python manage.py migrate
```

### Забули пароль адміністратора
**Рішення**: Створіть нового суперuser'а або скиньте базу даних (це видалить всі дані!):
```bash
python manage.py flush
python manage.py migrate
python manage.py createsuperuser
```

---

## 📱 Адреси на сайті

Коли сервер запущений, ви можете відвідати:

| Адреса | Опис |
|--------|------|
| `http://127.0.0.1:8000/` | Головна сторінка |
| `http://127.0.0.1:8000/routes/` | Список всіх маршрутів |
| `http://127.0.0.1:8000/routes/1/` | Деталі маршруту (замість 1 - ID маршруту) |
| `http://127.0.0.1:8000/register/` | Реєстрація нового користувача |
| `http://127.0.0.1:8000/login/` | Вхід |
| `http://127.0.0.1:8000/profile/username/` | Профіль користувача |
| `http://127.0.0.1:8000/admin/` | Панель адміністратора |

---

## 💡 Поради для новачків

1. **Спочатку додайте маршрути через панель адміністратора** на http://127.0.0.1:8000/admin/, щоб мати що показувати на сайті.

2. **Перевіряйте консоль** — якщо щось не працює, часто помилка буде видна в терміналі, де ви запустили сервер.

3. **Не чіпайте файл db.sqlite3** — це ваша база даних. Якщо його видалити, всі дані будуть втрачені.

4. **Регулярно робіть backup** — якщо ви додали багато важливих даних, скопіюйте файл db.sqlite3 на всякий випадок.

---

## 👤 Розробка

Проект розроблений Анастасією Шкварлою як платформа для спільноти любителів гірського туризму.

---

## 📄 Ліцензія

Цей проект відкритий для використання та модифікації.

---

## 🤝 Як допомогти проекту

Якщо у вас є ідеї щодо поліпшення сайту або ви знайшли помилку, будь ласка, повідомте про це!

**Можливі покращення:**
- Додати експорт маршрутів в GPX формат
- Реалізувати систему повідомлень між користувачами
- Додати можливість завантаження фотографій до відгуків
- Створити мобільний додаток
- Інтеграція з соціальними мережами

---

## 📞 Контакти

**Email**: info@mountain-routes.ua

Дякуємо за інтерес до нашого проекту! Приємних походів! 🥾⛰️